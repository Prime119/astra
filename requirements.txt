# ============================================================
#  Astra — dependencias
# ============================================================

# --- Núcleo (Fase 0) ---
psutil>=5.9          # detección de hardware (RAM/CPU)
httpx>=0.27          # cliente HTTP hacia el cerebro local (Ollama)

# --- Voz (Fase 1) ---
faster-whisper>=1.0  # oído / STT offline
sounddevice>=0.4     # captura y reproducción de audio
numpy>=1.24          # procesamiento de audio
piper-tts>=1.2       # voz / TTS offline (voz femenina en español)

# --- Memoria vectorial (Fase 2) — se activa más adelante ---
# chromadb>=0.5

# --- Visión / atención (Fase 7) ---
# opencv-python>=4.9
# mediapipe>=0.10

# --- Control del sistema (Fase 4) ---
# pyautogui>=0.9


# --- Interfaz Web (Fase Web) ---
aiohttp>=3.9           # servidor web para la interfaz JARVIS
